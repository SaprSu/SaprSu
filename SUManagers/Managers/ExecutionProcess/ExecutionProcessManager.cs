using System;
using System.Collections.Generic;
using System.Text;

namespace SUCore.Managers.ExecutionProcess
{
    public class ExecutionProcessManager
    {
        //ModulesActionExecuter _executer;
        //Queue<Guid> _execSequence;

        //public ExecutionProcessManager(ModulesActionExecuter executer, Queue<Guid> execSequence)
        //{
        //    _executer = executer;
        //    _execSequence = execSequence;
        //}

        //public void ExecuteAll()
        //{
        //    Queue<Guid> queue = new Queue<Guid>(_execSequence);
        //    while (queue.Count != 0)
        //    {
        //        _executer.ExecuteAction(queue.Dequeue());
        //    }
        //}

        //public Queue<Guid> ExecSequence
        //{
        //    get { return _execSequence; }
        //    set { _execSequence = value; }
        //}
    }
}
